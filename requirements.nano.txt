# =============================================================================
# requirements.nano.txt — Jetson Orin Nano deployment
#
# Use this file INSTEAD of requirements.txt on Jetson.
#
# torch, torchvision, tensorrt, and torch_tensorrt come pre-installed in the
# nvcr.io/nvidia/l4t-pytorch base image — do NOT pip-install them here or
# you will overwrite the ARM/L4T builds with broken x86 wheels.
#
# opencv-python is intentionally excluded: use the system python3-opencv
# package (installed via apt in Dockerfile.nano) which includes GStreamer
# and nvv4l2decoder support for hardware H.264 decoding on NVDEC.
#
# Python 3.10, JetPack 6.x (L4T r36.x), CUDA 12.2
# =============================================================================

# ---------------------------------------------------------------------------
# Core inference dependencies  (no torch, torchvision, opencv — see above)
# ---------------------------------------------------------------------------
numpy==2.2.6
pandas==2.3.3
omegaconf==2.3.0
ultralytics==8.4.45
gdown==6.0.0
scipy==1.15.3
pillow==12.2.0
# Imported at module level by src/utils/dataset.py (shipped, not on hot-path).
albumentations==2.0.8

# ---------------------------------------------------------------------------
# OSNet / torchreid dependencies
# ---------------------------------------------------------------------------
h5py==3.13.0
future==1.0.0
yacs==0.1.8
chardet==5.2.0
Cython==3.0.11
easydict==1.13
six==1.17.0
coloredlogs==15.0.1

# ---------------------------------------------------------------------------
# Config / utility
# ---------------------------------------------------------------------------
antlr4-python3-runtime==4.9.3
hydra-core==1.3.2
PyYAML==6.0.2
einops==0.8.2
tqdm==4.67.1
typing_extensions==4.15.0
filelock==3.18.0
fsspec==2025.3.2
requests==2.32.3
packaging==25.0

# ---------------------------------------------------------------------------
# Git-sourced dependencies
# ---------------------------------------------------------------------------
git+https://github.com/KaiyangZhou/deep-person-reid.git@f8cd150fdf77e8d9e1ed143b7f308c2c609ded50
git+https://github.com/facebookresearch/mobile-vision.git@bb8465cfe06327c35b398f3cc0b30e2e668eb0dd
